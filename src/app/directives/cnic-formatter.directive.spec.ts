import { CnicFormatterDirective } from './cnic-formatter.directive';

describe('CnicFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CnicFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
